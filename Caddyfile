{$DOMAIN} {
	root * /usr/share/caddy
	encode zstd gzip

	handle /api/* {
		reverse_proxy api:4000
	}

	handle /health {
		reverse_proxy api:4000
	}

	handle {
		try_files {path} /index.html
		file_server
	}
}
